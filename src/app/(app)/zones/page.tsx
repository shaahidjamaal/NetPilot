import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card"

export default function ZonesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Zones</CardTitle>
        <CardDescription>Manage service coverage zones.</CardDescription>
      </CardHeader>
       <CardContent>
        <p>Zones management content goes here.</p>
      </CardContent>
    </Card>
  )
}
